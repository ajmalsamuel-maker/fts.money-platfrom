/**
 * Temporary re-export for backwards compatibility
 * This allows old imports to still work during migration
 */

export { useI18n, SUPPORTED_LANGUAGES, I18nextProvider as EnhancedLanguageProvider } from './I18nextProvider';